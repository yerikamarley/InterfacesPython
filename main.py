# Importamos la función mostrar_interfaz1 desde el archivo interfaz1
from interfaz1 import mostrar_interfaz1 # Importamos la función mostrar_interfaz1 desde el archivo interfaz1

# Verificamos si el archivo actual se está ejecutando como programa principal
if __name__ == "__main__":  # Verificamos si el archivo actual se está ejecutando como programa principal   
    # Llamamos a la función mostrar_interfaz1 para mostrar la interfaz
    mostrar_interfaz1() # Llamamos a la función mostrar_interfaz1 para mostrar la interfaz
